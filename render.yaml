services:
  - type: web
    name: fitai-ai-clothing-system
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: FASHN_API_KEY
        sync: false
